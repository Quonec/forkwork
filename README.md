# ForkWork — гастрономическая платформа

**Foodtech-платформа для взаимодействия поваров и горожан**: интерактивная карта, live-стримы с открытым чатом, личные чаты по согласию, заказ блюд, внутренний кошелёк, рецепты, отзывы, AI-агент и админ-панель.

MVP реализован по техническому заданию как полнофункциональное веб-приложение.

---

## Быстрый старт

```bash
npm install
npm run dev        # http://localhost:3000 (разработка)

# или продакшен-сборка
npm run build
npm start
```

База данных SQLite создаётся и наполняется демо-данными **автоматически** при первом запуске (файл `data/forkwork.db`). Чтобы начать с чистого листа — просто удалите папку `data/`.

### Демо-аккаунты

| Роль | Email | Пароль | Что посмотреть |
|---|---|---|---|
| Заказчик | `user@forkwork.ru` | `user123` | Карта, корзина, заказы, кошелёк (5000 FC), избранное, личный чат с Нино |
| Повар | `chef@forkwork.ru` | `chef123` | Поварской кабинет: live-эфир, входящие заказы, запрос на чат, статистика |
| Админ | `admin@forkwork.ru` | `admin123` | Аналитика, модерация отзывов, жалобы, заявка «стать поваром» от Кирилла |

Ещё повара: `timur|nino|haru|polina|ayse|grisha|sofia@forkwork.ru` / `chef123`.

---

## Стек

- **Next.js 16** (App Router) + **TypeScript** + **Tailwind CSS 4**
- Дизайн в духе **Uber** на палитре светлой охры: плоские поверхности без градиентов и теней, чёрные основные кнопки, «залитые» серые инпуты, жирный гротеск с плотным трекингом для цифр и монограмм. Токены переопределены на уровне `@theme` в `globals.css` — сменить гамму можно в одном месте
- Юзабилити в том же духе: нижняя навигация на мобильных, плавающая панель корзины «N позиций · сумма → Оформить», крупный поиск в каталогах поваров и рецептов, списки-строки со стрелками

### Версии

| Версия | Где | Что это |
|---|---|---|
| **v0.3.0** (текущая) | ветка `main`, тег `v0.3.0` | Стиль и юзабилити в духе Uber |
| v0.1.0 «классика» | ветка `v1-classic`, тег `v0.1.0` | Исходный охристый стиль с монограммами Georgia |

Открыть старую версию: `git checkout v1-classic`.
- **SQLite** через встроенный `node:sqlite` — без нативных зависимостей и внешних сервисов
- **Leaflet + OpenStreetMap** — интерактивная карта
- Сессионная авторизация (httpOnly-cookie, bcrypt)
- Live-обновления чатов и стримов — поллинг (готово к замене на WebSocket)
- **AI-агент**: детерминированная эвристика из коробки + опциональная интеграция Claude API (`@anthropic-ai/sdk`) — задайте `ANTHROPIC_API_KEY`, и ответы ассистента генерирует модель `claude-opus-4-8`

## Соответствие техническому заданию

| Раздел ТЗ | Реализация |
|---|---|
| 5.1 Регистрация и авторизация | Email+пароль, выбор роли (повар/заказчик), смена роли через заявку с одобрением админа |
| 5.2 Первичный инструктаж | `/onboarding` — раздельные пошаговые сценарии для повара и заказчика |
| 5.3 Интерактивная карта | `/map` — маркеры поваров со статусом/рейтингом/LIVE-бейджем, фильтры: кухня, цена, рейтинг, доступность, live, доставка/самовывоз, поиск |
| 5.4 Профиль повара | `/chefs/[id]` — аватар, био, специализация, блюда, цены, режим работы, локация, стримы, рецепты, рейтинг, отзывы с ответами |
| 5.5 Поварской кабинет | `/kitchen` — 9 разделов: обзор-статистика, заказы, блюда (CRUD), рецепты, стримы, чаты, отзывы, профиль, финансы; тумблер «принимаю заказы» |
| 5.6 Кабинет заказчика | `/cabinet` — обзор, заказы, избранное, кошелёк, кнопка «Стать поваром»; рекомендации — через AI-виджет |
| 5.7 Стриминг | `/streams/[id]` — «эфир» с симуляцией активности, открытый чат, закреплённое сообщение, быстрые реакции, блок блюд эфира, заказ из стрима, запрос личного чата |
| 5.8 Чат | Открытый чат стрима + личные чаты `pending → active` по согласию повара, системные сообщения, история, блокировка, жалобы |
| 5.9 E-commerce | Корзина (один повар на заказ), оформление: доставка/самовывоз, оплата кошельком/картой(демо), статусы `новый→принят→готовится→доставляется→выполнен`, отмена с возвратом, история, комиссия платформы 10% |
| 5.10 Внутренний кошелёк | ForkCoins (1 FC = 1 ₽): пополнение, списание, чаевые, возвраты, история операций, антифрод-лимиты |
| 5.11 Отзывы и рейтинг | Оценка после выполненного заказа, средний рейтинг влияет на сортировку и карту, ответы повара, модерация (скрытие) |
| 5.12 AI-агент | Анализ эфира (распознанное блюдо + уверенность, сопоставление с репертуаром, теги, кнопка «Заказать») + ассистент-рекомендации по каталогу; финансовых решений не принимает |
| 7 Админ-панель | `/admin` — пользователи (блокировка), модерация отзывов, жалобы, заявки на роль, категории кухонь, остановка стримов |
| 8 Аналитика | События, GMV, комиссии, средний чек, конверсия стрим→заказ, выручка по поварам, источники трафика, заказы по дням, метрики чатов |

## Архитектура

```
src/
├── lib/
│   ├── db.ts          # node:sqlite: схема (17 таблиц), сид демо-данных, ленивый синглтон
│   ├── auth.ts        # сессии (cookie + таблица sessions), bcrypt
│   ├── api.ts         # json/err-хелперы, requireUser(role)
│   ├── queries.ts     # типизированные выборки (повара с рейтингом, стримы, рецепты)
│   ├── types.ts       # доменные типы, статусы заказов, комиссия
│   └── format.ts      # форматирование FC/дат, плюрализация
├── app/
│   ├── api/           # 23 REST-маршрута: auth, chefs, orders, wallet, chats,
│   │                  # streams/[id]/messages (с симуляцией зрителей), kitchen,
│   │                  # admin, ai/assistant, ai/stream-analysis, …
│   ├── map/           # карта (Leaflet, клиентские фильтры)
│   ├── streams/[id]/  # комната эфира (чат-поллинг, реакции, AI-блок)
│   ├── chefs/[id]/    # профиль повара (+корзина, избранное, чаевые, чат)
│   ├── kitchen/       # кабинет повара
│   ├── cabinet/       # кабинет заказчика
│   ├── admin/         # админ-панель
│   └── …              # лендинг, рецепты, корзина, заказы, чаты, онбординг
└── components/        # Header, карточки, корзина (Context+localStorage), AI-виджет
```

**Платёжная логика (демо-упрощение):** при оформлении заказа кошелёк заказчика дебетуется на полную сумму, повару сразу зачисляется сумма минус 10% комиссии; при отмене — зеркальный возврат. Все движения — в `transactions`.

**Симуляция live:** сообщения «зрителей» генерируются сервером по расписанию от момента старта эфира (идемпотентно через `bot_cursor`), счётчик зрителей «дышит». Реальный видеопоток подключается заменой блока плеера (HLS/WebRTC — например, LiveKit) без изменения остальной логики.

## Ограничения MVP и дорожная карта

- Видео стрима — анимированная заглушка → LiveKit/WebRTC
- Поллинг чатов (2–3 с) → WebSocket/SSE
- Платежи виртуальные → интеграция платёжного провайдера, эскроу вместо мгновенного зачисления
- AI-агент: эвристика + опциональный Claude → мультимодальный анализ видеопотока
- Геокодинг адресов вручную (lat/lng в профиле) → автоматический геокодер
- Восстановление пароля, телефон/соцсети — заглушки

> Платформа не заменяет санитарные и медицинские проверки; AI-агент — вспомогательный модуль; финансовая логика для продакшена требует юридической адаптации под страну запуска.
